// Authentication: identify if user is who they claim 
// Authorization: if the user has the right permission to perform an operation

// Register users: POST /api/users
// Login: a request/command (a new one) -> POST /api/logins (store individual logins)

// Implement register user api